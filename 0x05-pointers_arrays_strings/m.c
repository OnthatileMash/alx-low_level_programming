#include "main.h"

/**
 * _strcpy - copies string pointer, with the null byte to a dest pointer
 * @dest: char type string
 * @src: char type string
 * Return: chsr pointer
 */

char *_strcpy(char *dest, char *src)
{
	char arr_src[] = src;

	dest = arr_src;

	return (dest);
}
